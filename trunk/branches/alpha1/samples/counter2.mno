def int counter(int i)
  var int total = 0
  while (i > 0)
    total = total + 1
    i = i - 1
  end
  return(total)
end

action main(string[] args)
  var int i = convertToInt(args[0])
  var int total

  total = counter(i)

  puti(total)
  exit(0)
end
