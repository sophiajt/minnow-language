feature Bob
  x:int
end

actor Fred
  y:int

  action fred(y : Bob)
  end

  action fred(y : Bob, a : Bob, b : int)
    this.george(this.y)
  end

  def george(a:int)
  end
end

action main
  f : Fred = spawn Fred()
  z : Bob = new Bob()
  i : int = -4
  i = -4

  f::fred(z)
  f::fred(z)
  f::fred(z, z, 3+3)
  f::fred(z, z, i)
end
