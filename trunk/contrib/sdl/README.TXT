SDL module for Minnow.

To use: build like you would any cmake package.  Assuming you have installed any necessary libsdl-dev type packages, this should build into a library.  Keep this library where you're building your application.

Currently tested and known to work in Ubuntu Linux.
