def int[] passarray(int[] input)
  input[0] = input[0] + 3
  input[1] = input[1] + 3
  return(input)
end

action main()
  var int[] arr[2]
  arr[0] = 3
  arr[1] = 4
  arr = passarray(arr)
  puti(arr[0])
  puti(arr[1])

  exit(0)
end
