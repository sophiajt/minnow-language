action main
  x : var = "Bob"
  y : var = x
  z : var
  z = 'C' + y + x + x
  print(z + '\n')
  if (z == "CBobBobBob")
    print("match\n")
  else
    print("no match\n")
  end
  if (z != "Frog")
    print("good\n")
  end
  Sys.exit()
end
